# Test plan (Flutter)

Put the test ID in each test description, e.g. `test('U-15 one Show marks direction learning', …)`.

## Unit tests — `test/domain/` (pure Dart)

### Importer and POS
| ID | Case | Expected |
|---|---|---|
| U-1 | `fixtures/definitions.txt` | equals `fixtures/expected/definitions.txt.json` (dash; 998 words; noun 501, adjective 336, verb 149, adverb 12) |
| U-2 | `fixtures/edge-cases.txt` | equals golden (9 words; line 11 `NO_SEPARATOR`; 1 duplicate) |
| U-3 | `fixtures/sample-excel-dash.csv` (BOM, CRLF, quoted lines) | equals golden (60 words, commas kept, no stray quotes) |
| U-4 | `fixtures/sample-columns.csv` | equals golden (columns; 60 words; noun 24, adjective 20, verb 16) |
| U-5 | Columns CSV without header, 2 columns | term + definition; POS from a leading `n.` if present |
| U-6 | Semicolon- and tab-delimited CSV | parsed like comma |
| U-7 | Windows-1252 bytes containing `é` (0xE9) | decoded correctly |
| U-8 | Definition containing `" - "` | only the first separator splits |
| U-9 | `nopos - a definition…` | no POS; definition unchanged |
| U-10 | POS labels | `j`→`adj.`, `a`→`adv.`, `n`→`noun`, `prep`→`prep.`, unknown column value `det`→`det.` |

### Packing
| ID | Case | Expected |
|---|---|---|
| U-11 | 998 words, size 30 | 34 packs, last has 8, consecutive positions |
| U-12 | Rebuild to 20: old pack 1 mastered WD+DW, old pack 2 mastered WD only, no other reveals | new pack 1 (words 0–19) learned; pack 2 (20–39): WD mastered, DW learning; pack 3 (40–59): WD mastered, DW unseen → status learning; pack 4 new; 50 packs |
| U-13 | Rebuild invariants | every word in exactly one pack; numbers 1..k; sizes ≤ 20 |

### Learning
| ID | Case | Expected |
|---|---|---|
| U-14 | Pass with only Next | completed, clean; mastery → mastered; `PassCompleted(clean: true)` |
| U-15 | One Show | `Revealed` event; completed pass not clean |
| U-16 | Show twice on same card | one reveal |
| U-17 | Previous onto revealed / unrevealed card | `currentRevealed` true / false; no new reveal |
| U-18 | Card order | always pack words in source order |
| U-19 | Worked example (LEARNING_LOGIC §2) | each row's status matches |
| U-20 | Learned rule `either` / `wdOnly` / `dwOnly` | status per rule |
| U-21 | Review with `demoteOnReveal` off / on | stays learned / becomes learning |
| U-22 | `continueTarget` priority | open pass > learning > new > none; direction choice |

### Categories and tags
| ID | Case | Expected |
|---|---|---|
| U-23 | `"  Law "` then `"law"` | same key; display name "Law" |
| U-24 | Same subcategory name under two categories | allowed |
| U-25 | Subcategory without category / with wrong parent | rejected |
| U-26 | Tone select, reselect, switch | set / cleared / replaced; traits unaffected |

## Repository and service tests — `test/data/`, `test/services/` (in-memory drift)

| ID | Scenario |
|---|---|
| R-1 | Commit each fixture → wordlist, words, packs match `fixtures/README.md` |
| R-2 | Empty file → `NO_WORDS_FOUND`; file > 5 MB rejected |
| R-3 | Delete wordlist removes words, packs, passes; categories remain |
| R-4 | Close and reopen a file-backed database → pass state and data intact |
| R-5 | Every service method runs in one transaction (inject a failure mid-way → no partial writes) |
| R-6 | Worked example via `LearningService` → pack learned, `becameLearned` true exactly once |
| R-7 | `TaggingService`: categorising a non-learned word → `WordNotLearnedException`; tone and traits on any word → saved (D-32) |
| R-8 | `CategoryService.assign(word, 'Emotions', 'Anger')` find-or-create, case-insensitive; wrong parent → `InvalidSubcategoryException` |
| R-9 | `applyPackSizeChange(20)` → U-12 expectations in the DB; open passes deleted |
| R-10 | Changing learned rule changes derived pack statuses with no writes |
| R-11 | Tree streams: counts correct and update after tagging; node word ordering rules |
| R-12 | Backup export → restore into empty DB → identical data |
| R-13 | Migration test from schema v1 (generated by drift) |

## Widget tests — `test/widgets/`

| ID | Widget | Checks |
|---|---|---|
| W-1 | `WordCard` | WD shows term only; DW definition only; Show reveals without changing the button positions (compare `tester.getRect`) |
| W-2 | `WordCard` POS | `adj.` chip with semantics label "adjective" |
| W-3 | Learn screen | Show → "Shown" disabled; peek counter increments; summary appears after last Next |
| W-4 | `QuickTagBar` | tone single-select with clear; traits independent; semantics labels |
| W-5 | Tag visuals | correct border/tint/badges/dashed outline for combinations, light and dark (`WpColors`); existing tags still shown on a word whose pack is no longer Learned, with no tag controls |
| W-6 | `CategoryRow` | filters case-insensitively; `Create "x"` only when no exact match; subcategory disabled until category chosen |

## Integration tests — `integration_test/` (device/emulator and desktop)

| ID | Scenario |
|---|---|
| I-1 | Import `definitions.txt` (inject the file through a fake picker) → 998 words, 34 packs, all New; POS shown as "adj."/"adv." |
| I-2 | Pack 1 WD: Next ×30 → "Clean pass"; WD mastered; pack Learning |
| I-3 | Pack 1 DW: Show on card 5, finish → 1 peek; repeat with Next only → "Pack 1 learned!"; tree icon green; no categorise prompt appears |
| I-4 | Restart app mid-pass at card 12 with one peek → resume banner → card 12, peeks 1 |
| I-5 | Switch direction mid-pass → confirm → card 1 in DW |
| I-6 | Learning pack 2 (not learned): quick tag buttons present and working (also keys 1–5); no category row, C does nothing |
| I-7 | Review pack 1: on `accuse` tap Negative, then Counter-intuitive → red border, dashed outline, badge; tap Negative again → border gone, badge stays |
| I-8 | Review pack 1: `abbey` → `Religion › Places`; on `acclaim` the category field suggests `Religion`, then `Places` |
| I-9 | Explorer: select `Religion › Places` → `1 / 2`, swipe → `2 / 2`, reveal works, pack statuses unchanged |
| I-10 | Phone size (360×780): drawer tree, swipe Next, quick buttons on two rows, targets ≥ 48 dp |
| I-11 | Settings: pack size 20 → dialog counts → rebuild → pack 1 (20 words) still learned |

## Accessibility
- `expect(tester, meetsGuideline(androidTapTargetGuideline))`, `labeledTapTargetGuideline`, `textContrastGuideline` on learn, review, explorer and settings screens.
- Manual: TalkBack and VoiceOver run of I-2 and I-7; text scale 200 %; keyboard-only run on desktop.
